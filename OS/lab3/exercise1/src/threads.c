#include <stdio.h>
#include <pthread.h>

//global variable
double num;

void userInput() {
    while(1) {
        printf("Enter a number: ");
        scanf("%lf", &num);
        if (num != )
    }
}

int main () {

  

  return 0;
}